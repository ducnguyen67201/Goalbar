{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum permissions for the Tagline main window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
